version: 2
models:
  - name: stg_prices
    description: "Données Bigtable nettoyées"
    columns:
      - name: product_id
        tests: [not_null, unique]
      - name: price
        tests: [not_null]
      - name: site
        tests: [not_null]
