name: 'price_intelligence'
version: '1.0.0'
config-version: 2

profile: 'price_intelligence'

model-paths: ["models"]
test-paths: ["tests"]
docs-paths: ["docs"]

models:
  price_intelligence:
    staging:
      +materialized: view
    marts:
      +materialized: table